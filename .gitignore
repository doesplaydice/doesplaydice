# --- Build output -----------------------------------------------------------
.venv/
wiki/site/
# Firebase deploy cache. Staging was retired on 2026-09-09 -- it was serving a
# fully ungated copy of the site, which defeated the pre-launch passphrase.
# GitHub Pages is the only host now.
.firebase/

# Note: kit/dist/*.pdf IS committed on purpose. They regenerate with
# ./kit/build.sh, but keeping them in the repo means the download links work
# immediately and Jeff can grab a printable file without running anything.

# --- Jeff's private research ------------------------------------------------
# EXCLUDED ON PURPOSE. Two reasons, both serious:
#   1. It is ~295k words of Jeff's personal notes, not project source.
#   2. Rules/Examples/ contains long verbatim passages from other publishers
#      (Pathfinder 2e, D&D 4e, Lamentations of the Flame Princess). Fine as
#      private research, absolutely not publishable.
# This repo is intended to go public. Do not remove this line.
files-from-jeff/

# --- Client deliverables (not project source) -------------------------------
does-play-dice-packet.html
# Drafts and one-pagers written for Jeff and for Andrei's own use: the explainer
# script, the review brief, the briefing sheet. Kept on disk so they survive a
# new chat session, out of git because they are not the game.
deliverables/

# --- OS ---------------------------------------------------------------------
.DS_Store
