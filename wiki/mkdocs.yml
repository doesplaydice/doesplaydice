# ---------------------------------------------------------------------------
# PROTOTYPE — built for the Jeff Adams strategy call.
# Everything marked "OPEN DECISION" in the content is deliberately unresolved.
# ---------------------------------------------------------------------------

site_name: Does Play Dice
site_description: A story-telling game that's free to download, quick to learn, and only limited by your imagination.
site_url: https://doesplaydice.com/

# PLACEHOLDER — repo does not exist yet. Drives the "Edit this page" pencil,
# which is the whole "Jeff can update it himself" story.
repo_url: https://github.com/doesplaydice/doesplaydice
repo_name: doesplaydice/doesplaydice
edit_uri: edit/main/wiki/docs/

theme:
  name: material
  language: en
  # TEMPORARY: pre-launch gate. Remove custom_dir and wiki/overrides/ at go-live.
  custom_dir: overrides
  # Fonts are self-hosted -- see assets/stylesheets/fonts.css.
  # `false` stops Material requesting anything from Google.
  font: false
  icon:
    logo: material/dice-d20
    repo: fontawesome/brands/github
    edit: material/pencil
  palette:
    # Dark first and no media query, so dark is the default with no stored
    # preference. The design is dark-first by intent.
    - scheme: slate
      primary: custom
      accent: custom
      toggle:
        icon: material/moon-waning-crescent
        name: Switch to light
    - scheme: default
      primary: custom
      accent: custom
      toggle:
        icon: material/white-balance-sunny
        name: Switch to terminal
    # A third state, and not a joke at the expense of the design: the game's
    # whole premise is a rules system you can hold in your head, and Jeff's
    # motivation is the 2023 OGL revocation. A phosphor CRT is the right kind
    # of nerd. Material takes an arbitrary scheme name here and simply sets
    # data-md-color-scheme, so the styling is ours.
    - scheme: terminal
      primary: custom
      accent: custom
      toggle:
        icon: material/television-classic
        name: Switch to dark
  features:
    - navigation.instant
    - navigation.tracking
    - navigation.tabs
    - navigation.sections
    - navigation.top
    - navigation.footer
    - toc.follow
    - search.suggest
    - search.highlight
    - search.share
    - content.action.edit
    - content.tooltips

extra_css:
  - assets/stylesheets/fonts.css
  - assets/stylesheets/extra.v3.css
extra_javascript:
  # Marks the plan timeline and computes the day count in the browser, so the
  # countdown cannot go stale the way a written-in number does.
  - assets/js/timeline.js
  # The try-it roller on the rules page.
  - assets/js/dice.js
  # Registers the service worker. Kept out of the template so it survives the
  # pre-launch teardown -- offline support is permanent, the gate is not.
  - assets/js/offline.js
  # The quick roller on the Narrator's screen.
  - assets/js/screen.js
  # Writes the filled-in character sheet to a PDF in the browser. Loaded first
  # so sheet.js can tell whether the Download button has anything behind it.
  - assets/js/sheet-pdf.js
  # Makes the character sheet typeable in the browser.
  - assets/js/sheet.js

markdown_extensions:
  - abbr
  - admonition
  - attr_list
  - def_list
  - footnotes
  - md_in_html
  - tables
  - toc:
      # Material renders this as the right sidebar's heading.
      # "On this page" contrasts with the section name on the left.
      title: On this page
      permalink: true
  - pymdownx.details
  - pymdownx.superfences
  - pymdownx.tabbed:
      alternate_style: true
  - pymdownx.emoji:
      emoji_index: !!python/name:material.extensions.emoji.twemoji
      emoji_generator: !!python/name:material.extensions.emoji.to_svg

# The fonts README is for people reading the repo, not a page on the site.
# CNAME must reach the built site, so it is NOT excluded -- GitHub Pages reads
# it from the deployed artifact, and without it a deploy clears the custom domain.
exclude_docs: |
  assets/fonts/README.md

plugins:
  - search

extra:
  social:
    - icon: material/web
      link: https://doesplaydice.com
      name: doesplaydice.com

copyright: >
  Does Play Dice by Jeff Adams &middot; text licensed
  <a href="https://creativecommons.org/licenses/by/4.0/" rel="license">CC BY 4.0</a>
  &middot; &ldquo;Does Play Dice&rdquo; and the &ldquo;Does Play Dice&rdquo; logo are excluded

nav:
  - Home: index.md
  - The Rules:
      - Basic Rules (complete): rules/index.md
      - The odds: rules/odds.md
      - "Taking actions (draft)": rules/taking-actions.md
      - "Combat (draft)": rules/combat.md
      - "Harm and recovery (draft)": rules/harm.md
      - "Getting better (draft)": rules/training.md
      - "Paths (draft)": rules/paths.md
      - "The three kinds of play (draft)": rules/pillars.md
      - "Effects (draft)": rules/effects.md
      - Character Vitae: rules/character-vitae.md
      - Expanded Rules: rules/expanded.md
  - The World:
      - About the setting: world/index.md
  - For Narrators:
      - Running a game: narrators/index.md
      - The screen: narrators/screen.md
  - Launch plan:
      - Overview: plan/index.md
      - Open decisions: decisions.md
      - Week by week: plan/weeks.md
      - How to run a playtest: plan/playtesting.md
      - The five tests: plan/tests.md
      - Is the test any good?: plan/validity.md
      - Progress log: plan/progress.md
  - Community:
      - Modules: community/index.md
      - "Sample: The Long Way Round": community/modules/sample-module.md
  - Take it: fork.md
  - Contribute:
      - How to contribute: contribute/index.md
  - Download: downloads.md
